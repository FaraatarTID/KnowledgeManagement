# Public API base URL used by the browser.
# Keep `/api/v1` suffix so client requests resolve correctly.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Optional internal URL used by Next.js server rewrites/proxy.
# When omitted, the app derives this from NEXT_PUBLIC_API_URL.
API_INTERNAL_URL=http://localhost:3001
