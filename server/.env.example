# --- AIKB SERVER CONFIGURATION ---
# Copy this file to .env and adjust as needed.

# Environment (development/production)
NODE_ENV=development
PORT=3001

# SECURITY: Secret key for signing login sessions. 
# Change this to a long random string for production.
JWT_SECRET=development-secret-change-me

# --- BOOTSTRAP ADMIN ---
# Used to create the very first user on a fresh database.
# Default: alice@aikb.com
INITIAL_ADMIN_EMAIL=alice@aikb.com
INITIAL_ADMIN_PASSWORD=admin123
INITIAL_ADMIN_NAME=System Administrator

# --- AI CONFIGURATION (RAG) ---
# Choose Mode: 'LOCAL' (Easy) or 'VERTEX' (Enterprise)
VECTOR_STORE_MODE=LOCAL

# Option 1: Easy Mode (Used if VECTOR_STORE_MODE=LOCAL)
# Get a GEMINI API KEY from https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your-gemini-key-here

# Option 2: Enterprise Mode (Used if VECTOR_STORE_MODE=VERTEX)
GOOGLE_CLOUD_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1

# --- GOOGLE DRIVE CONNECTION ---
# Requires a service account JSON file (server/data/google-key.json)
GOOGLE_DRIVE_FOLDER_ID=your-drive-folder-id-here
GCP_KEY_FILE=server/data/google-key.json

# --- DATABASE (SUPABASE) ---
# Required for production persistence and audit logs.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# --- AI MODELS ---
GEMINI_MODEL=gemini-2.5-flash-lite-001
EMBEDDING_MODEL=text-embedding-004

# --- SECURITY ---
# Comma-separated list of allowed frontend URLs
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# --- OBSERVABILITY ---
# Optional: Sentry DSN for server error tracking
SENTRY_DSN=
